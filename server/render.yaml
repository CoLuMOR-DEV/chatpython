services:
  - type: web
    name: console-chat-server
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --worker-class eventlet -w 1 app:app
    envVars:
      - key: ADMIN_KEY
        generateValue: true
      - key: PYTHON_VERSION
        value: 3.11.9
